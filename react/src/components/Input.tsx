// react components must start with a capital letter, 
// otherwise React will treat them as HTML elements and throw an error

function Input() {
    return (
        <input type="text" placeholder="Type something..." className="myInput" />

    );
}

export default Input;